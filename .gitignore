node_modules/
.env
.claude/worktrees/
data/output/
data/landingpages/
data/betrieb/
data/lead-edits/
data/resonanz/
public/uploads/
.preview/

# v2: Zwischenstände der Judge-Runden (Screenshots je Runde) und KI-Medien-Cache
v2/output/judge/
v2/output/medien/
# v2: Entwürfe echter Leads (Kundendaten) und die lokale Engine-Wahl
v2/output/leads/
# Interne Build-Berichte der öffentlichen Beispielseiten (nicht nach docs/)
v2/output/berichte/
# Sicherungen (z. B. der abgeschalteten öffentlichen Lead-Demos) – enthalten Namen echter Betriebe
data/sicherung/
data/v2-engine.json

# Referenz-Frames fremder Websites (nur lokal, nicht veröffentlichen – Repo ist für GitHub Pages öffentlich)
.referenzen/
data/seitenaufrufe/
data/kunden/
v2/output/kunden/

# Personenbezogene bzw. Lead-Daten, die zur Laufzeit entstehen (Audit 25.09.2026):
# Telefonnummern nicht erschienener Gäste, gelernte Wartezeiten,
# Farbschema-Wahl je Lead (Place-IDs), KI-Texte je Lead, Briefings echter Betriebe
data/zuverlaessigkeit/
data/wartezeitLernen/
data/stimmungen.json
v2/output/copy/
v2/briefings/*
!v2/briefings/pilot-*.json
# Lokale Umgebungsdateien jeder Art
.env.*
!.env.example

# Veröffentlichungspakete von Kundenfassungen (npm run kunde -- paket)
v2/output/pakete/
